#include "header.h"


int parse_url(const char *url, url_t *parsed) {
    // Find the scheme separator

    // Skip scheme (http://)

    // Find host and port

    // Check for port

    // Find path

    // Find query and fragment

    return 0;
}